plugins {
    id 'java'
}

group = 'id.diasna.refactor'
version = '0.1'

repositories {
    mavenCentral()
}

java {
    sourceCompatibility = JavaVersion.toVersion("16")
    targetCompatibility = JavaVersion.toVersion("16")
}

dependencies {

}